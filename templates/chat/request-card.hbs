<div class="skjaald2 chat-card request-card">
    <div class="card-buttons">
        <button {{skjaald-dataset dataset}}>
            <span class="visible-dc">{{{buttonLabel}}}</span>
            <span class="hidden-dc">{{{hiddenLabel}}}</span>
        </button>
    </div>
</div>
