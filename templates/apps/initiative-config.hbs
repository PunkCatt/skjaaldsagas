<form autocomplete="off">
    <p class="notes">{{localize "SKJAALD.InitiativeConfigHint"}}</p>

    <div class="form-group">
        <label>{{localize "SKJAALD.AbilityModifier"}}</label>
        <div class="form-fields">
            <select name="system.attributes.init.ability">
                {{selectOptions abilities selected=ability blank="" labelAttr="label"}}
            </select>
        </div>
    </div>

    <div class="form-group">
        <label>{{localize "SKJAALD.InitiativeBonus"}}</label>
        <div class="form-fields">
            <input name="system.attributes.init.bonus" type="text" value="{{bonus}}">
        </div>
    </div>

    <div class="form-group">
        <label>{{localize "SKJAALD.FlagsAlert"}}</label>
        <input type="checkbox" name="flags.skjaald.initiativeAlert" {{checked initiativeAlert}}>
    </div>

    <div class="form-group">
        <label>{{localize "SKJAALD.FlagsInitiativeAdv"}}</label>
        <input type="checkbox" name="flags.skjaald.initiativeAdv" {{checked initiativeAdv}}>
    </div>

    <button type="submit"><i class="far fa-save"></i> {{ localize "Submit"}}</button>
</form>
