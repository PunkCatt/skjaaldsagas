<div class="tab" data-group="appearance" data-tab="ring">
    <div class="form-group">
        <label>{{localize "SKJAALD.TokenRings.Enabled"}}</label>
        <div class="form-fields">
            <input type="checkbox" name="flags.skjaald.tokenRing.enabled" {{checked flags.enabled}}>
        </div>
    </div>
    <div class="form-group">
        <label>{{localize "SKJAALD.TokenRings.Subject.Label"}}</label>
        <div class="form-fields">
            {{filePicker type="image" target="flags.skjaald.tokenRing.textures.subject"}}
            <input class="image" type="text" name="flags.skjaald.tokenRing.textures.subject"
                   value="{{flags.textures.subject}}" placeholder="{{subjectPlaceholder}}">
        </div>
        <p class="hint">{{localize "SKJAALD.TokenRings.Subject.Hint"}}</p>
    </div>
    <div class="form-group">
        <label>{{localize "SKJAALD.TokenRings.ScaleCorrection"}}</label>
        <div class="form-fields">
            {{rangePicker name="flags.skjaald.tokenRing.scaleCorrection" value=flags.scaleCorrection
                          min=0.8 max=4 step=0.1}}
        </div>
    </div>
    <div class="form-group">
        <label>{{localize "SKJAALD.TokenRings.RingColor"}}</label>
        <div class="form-fields">
            {{colorPicker name="flags.skjaald.tokenRing.colors.ring" value=flags.colors.ring}}
        </div>
    </div>
    <div class="form-group">
        <label>{{localize "SKJAALD.TokenRings.BackgroundColor"}}</label>
        <div class="form-fields">
            {{colorPicker name="flags.skjaald.tokenRing.colors.background" value=flags.colors.background}}
        </div>
    </div>
    {{#each effects}}
    <div class="form-group">
        <label>{{label}}</label>
        <div class="form-fields">
            <input type="checkbox" name="flags.skjaald.tokenRing.effects.{{@key}}" {{checked checked}}>
        </div>
    </div>
    {{/each}}
</div>
