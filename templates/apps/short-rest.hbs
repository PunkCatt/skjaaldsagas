<form id="short-rest-hd" class="dialog-content" onsubmit="event.preventDefault();">
    <p>
        {{#if isGroup}}{{ localize "SKJAALD.ShortRestHintGroup" }}
        {{else}}{{ localize "SKJAALD.ShortRestHint" }}{{/if}}
    </p>

    {{#if availableHD}}
    <div class="form-group">
        <label>{{ localize "SKJAALD.ShortRestSelect" }}</label>
        <div class="form-fields">
            <select name="hd">
                {{#select denomination}}
                {{#each availableHD as |num denom|}}
                <option value="{{denom}}">{{denom}} ({{num}} {{ localize "SKJAALD.available" }})</option>
                {{/each}}
                {{/select}}
            </select>
            <button id="roll-hd" {{#unless canRoll}}disabled{{/unless}}>
                <i class="fas fa-dice-d20"></i> {{ localize "SKJAALD.Roll" }}
            </button>
        </div>
        {{#unless canRoll}}
        <p class="notes">{{ localize "SKJAALD.ShortRestNoHD" }}</p>
        {{/unless}}
    </div>
    {{else}}
    <div class="form-group">
        <label>{{ localize "SKJAALD.HitDiceAutoSpend.Label" }}</label>
        <input type="checkbox" name="autoHD">
        <p class="hint">{{localize "SKJAALD.HitDiceAutoSpend.Hint" }}</p>
    </div>
    {{/if}}

    {{#if promptNewDay}}
    <div class="form-group">
        <label>{{ localize "SKJAALD.NewDay" }}</label>
        <input type="checkbox" name="newDay" {{checked newDay}}>
        <p class="hint">{{ localize "SKJAALD.NewDayHint" }}</p>
    </div>
    {{/if}}

    <div class="dialog-buttons">
        {{#each buttons as |button id|}}
        <button class="dialog-button" data-button="{{id}}">
            {{{button.icon}}}
            {{{button.label}}}
        </button>
        {{/each}}
    </div>
</form>
