<form autocomplete="off">
    <h3>{{ localize "SKJAALD.DamageTypes" }}</h3>
    <p class="note">{{ localize "SKJAALD.DamageModification.Hint" }}</p>
    <ol class="trait-list modifications">
        {{#each modifications}}
        <li>
            <label>
                {{ label }}
                <input type="text" name="system.traits.dm.amount.{{ @key }}" value="{{ value }}"
            </label>
        </li>
        {{/each}}
    </ol>

    <h3>{{ localize "SKJAALD.DamagePhysicalBypass" }}</h3>
    <p class="note">{{ localize "SKJAALD.DamageModification.BypassHint" }}</p>
    {{> "skjaald.trait-list" choices=bypasses prefix="system.traits.dm.bypasses" }}

    <button type="submit" name="submit" value="1">
        <i class="fa-regular fa-save"></i> {{ localize "SKJAALD.TraitSave" }}
    </button>
</form>
