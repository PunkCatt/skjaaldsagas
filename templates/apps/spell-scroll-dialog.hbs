<form>
    <div class="form-group">
        <label>{{ localize "TYPES.Item.spell" }}</label>
        <div class="form-fields">
            {{{ anchor }}}
        </div>
    </div>
    <div class="form-group">
        <label>{{ localize "SKJAALD.Scroll.Explanation.Label" }}</label>
        <div class="form-fields">
            <select name="explanation">
                {{#select explanation}}
                <option value="full">{{ localize "SKJAALD.Scroll.Explanation.Complete" }}</option>
                <option value="reference">{{ localize "SKJAALD.Scroll.Explanation.Reference" }}</option>
                <option value="none">{{ localize "SKJAALD.None" }}</option>
                {{/select}}
            </select>
        </div>
        <p class="hint">{{ localize "SKJAALD.Scroll.Explanation.Hint" }}</p>
    </div>
    <div class="form-group">
        <label>{{ localize "SKJAALD.SpellLevel" }}</label>
        <div class="form-fields">
            <select name="level" data-dtype="Number">
                {{ selectOptions spellLevels selected=level name="level" }}
            </select>
        </div>
    </div>
</form>
