<form autocomplete="off">
    <header>{{ localize "SKJAALD.Award.Title" }}</header>
    <div class="distribution">
        <span>{{localize "SKJAALD.Award.Distribution.Split"}}</span>
        <slide-toggle name="each" {{checked each}}></slide-toggle>
        <span>{{localize "SKJAALD.Award.Distribution.Each"}}</span>
    </div>
    {{#unless hideXP}}
    <label class="xp currency-style">
        <span class="roboto-upper">{{localize "SKJAALD.ExperiencePoints"}}</span>
        {{numberInput xp name="xp" class="uninput" min=0 step=1}}
    </label>
    {{/unless}}
    <section class="currency">
        {{#each currency}}
        <label aria-label="{{label}}">
            <i class="currency {{ @key }}" data-tooltip="{{ label }}"></i>
            {{numberInput value name=(concat "amount." @key) class="uninput" min=0 step=1}}
        </label>
        {{/each}}
    </section>
    <div class="destinations">
        {{#if noPrimaryParty}}
        <p class="no-primary-party">{{localize "SKJAALD.Award.NoPrimaryParty"}}</p>
        {{/if}}
        {{#each destinations}}
        <label>
            <input type="checkbox" name="destination.{{doc.id}}" {{checked checked}}>
            {{{icon}}} {{doc.name}}
        </label>
        {{else}}
        <p class="no-destinations">{{localize "SKJAALD.Award.NoDestinations"}}</p>
        {{/each}}
    </div>
    <button type="submit" name="transfer" disabled>
        <i class="fa-solid fa-trophy"></i> {{localize "SKJAALD.Award.Action"}}
    </button>
</form>
